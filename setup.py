from setuptools import setup, find_packages

setup(
    name="travel_agent",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # your dependencies will be read from requirements.txt
    ]
)